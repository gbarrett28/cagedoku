// 'UniqueRectangle' is disabled because its proof depends on the puzzle having a
// unique solution — a meta-assumption the rest of the rule set does not make.
// Unlike the other entries this list normally holds, it is not here because the
// rule produces eliminations contradicting a golden solution.
export const DISABLED_RULES: readonly string[] = ['UniqueRectangle'];
