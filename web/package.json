{
  "name": "killer-sudoku-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build && node scripts/generate-sw.js",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vitest": "^1.6.0"
  }
}
